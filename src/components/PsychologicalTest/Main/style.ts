import styled from "styled-components";

export const PsyTestLayout = styled.div`
    min-width: 100vw;
    min-height: calc(100vh - 3vw);

    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
`

export const TestVideo = styled.video`
    
`
